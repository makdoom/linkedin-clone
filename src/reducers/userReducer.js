// initial state
const INITIAL_STATE = {
  user: null,
};

// reducer
const userReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    //   case goes here
    default:
      return state;
  }
};
